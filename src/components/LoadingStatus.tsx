const LoadingStatus = () => {
  return (
    <div style={{ textAlign: 'center', margin: '20px' }}>
      <span
        style={{
          textAlign: 'center',
          padding: '10px',
          border: '1px dotted grey',
          borderRadius: '15px',
        }}
      >
        Loading response!
      </span>
    </div>
  );
};

export default LoadingStatus;
